VITE_API_URL=http://127.0.0.1:8000
# For production deployment:
# VITE_API_URL=https://api.yourdomain.com
# Keep 127.0.0.1 for local dev to match uvicorn default host and avoid localhost/IPv6 mismatch
